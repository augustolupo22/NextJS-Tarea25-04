import RecipeGrid from "./RecipeGrid";

const HomeContainer = () => {
  return (
    <header id="hero">
      <h1 class="title">Sencillo, pero delicioso</h1>
      <section class="recetas_grid"></section>
    </header>
  );
};

export default HomeContainer;
